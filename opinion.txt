/*
 * 1. 不需要使用智能指针,因为库对于上层应用来说如果参数正确一定是运行正确,
 *    不需要考虑中间因为什么函数退出导致内存泄露,new,free一一对应即可
 * 2. 如果出现两次Create，一次resume的话在resume中取切换线程不就有问题了吗，
 *    在调用栈中取的时候可能两个协程实体都未运行，就导致了问题。
 * 3. 因为第二条和一些resume中的一些条件判断,我认为把resume和Create和为一个函数是极好的
 * 4. 共享栈中多栈可以使得我们在进程切换的时候减少拷贝次数
 * 5. 对于协程切换时保存全部栈内容需要new这一点，我们是否可以引入一个内存池来优化呢？
 * */